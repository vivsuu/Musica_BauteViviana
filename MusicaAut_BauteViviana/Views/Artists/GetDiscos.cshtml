@model IEnumerable<MusicaAut_BauteViviana.Models.Album>

@{
    ViewData["Title"] = "GetDiscos";
}
 
<h1>Discos de @ViewBag.Name</h1>
<p>
    <a asp-controller="Albums" asp-action="Create">Crear nuevo álbum</a>
</p>


<table class="table">
    <thead>
        <tr>
            <th>
                Título del álbum
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
@*             <td>
                <a asp-action="Edit" asp-route-id="@item.AlbumId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AlbumId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AlbumId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
